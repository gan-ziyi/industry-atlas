# 安全说明

## 不应提交的内容

- `backend/.env` 和其他真实环境配置；
- DeepSeek API Key、微信 AppSecret、会话令牌和密码；
- `backend/data/` 中的数据库、上传年报和用户研究数据；
- 本机备份、日志、浏览器资料和构建缓存。

仓库已经通过 `.gitignore` 排除这些文件，但提交前仍应检查暂存区。

## 发现密钥泄漏

不要只删除 GitHub 页面中的文件。请先在对应服务商控制台撤销泄漏的密钥并生成新密钥，再清理 Git 历史。已经公开过的密钥应视为不再安全。

## 漏洞反馈

在仓库公开前，请通过仓库维护者指定的私密渠道反馈安全问题，不要在公开 Issue 中粘贴密钥、真实年报数据或可利用细节。
