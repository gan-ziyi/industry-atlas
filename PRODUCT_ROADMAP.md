# Industry Atlas 产品化路线

## 当前判断

现有版本已经完成产品交互 MVP，适合继续作为需求验证和界面原型。正式产品不应继续在单个 HTML 文件上堆叠账号、文件解析和多人权限，而应保留当前交互设计，重建为“多端前端 + 业务后端 + 异步文档处理”的结构。

## 推荐技术架构

### 客户端

- 第一正式端：响应式 Web 应用，便于处理大图谱、上传文件和长时间研究。
- 微信入口：使用 Taro + React 制作小程序，共享业务组件和接口类型。
- 小程序优先承担查看、轻编辑、任务处理和资料上传；复杂图谱编辑在 Web 端体验更好。
- 当前原型继续作为 UI 和交互规范，不直接承担正式数据存储。

### 后端

- API：Python FastAPI。它更适合后续 PDF、OCR、表格和 AI 提取任务。
- 数据库：PostgreSQL；后续需要语义检索时启用 pgvector，无需一开始增加独立向量数据库。
- 文件：腾讯云 COS、阿里云 OSS 或兼容 S3 的对象存储。
- 异步任务：Redis + Celery/RQ，用于年报解析、OCR、AI 提取和报告生成。
- AI：由后端统一调用 DeepSeek，API Key 只保存在服务器环境变量或密钥服务中。

## 核心数据模型

- `users`：用户资料与登录身份。
- `workspaces`：个人或团队研究空间。
- `workspace_members`：成员和角色。
- `projects`：产业研究项目。
- `nodes`：产业节点、研究状态、解释和重要性。
- `edges`：结构、供应、依赖、制约、替代和受益关系。
- `sources`：年报、研报、新闻、网页和文件元数据。
- `source_chunks`：带页码、章节、原文位置的文本片段。
- `claim_citations`：具体结论与原文片段的关联。
- `research_tasks`：节点任务、优先级和状态。
- `ai_runs`：每次 DeepSeek 请求、模型、提示版本、耗时和费用。
- `patches`：AI 提出的修改及其接受、拒绝状态。
- `snapshots`：项目版本与恢复点。
- `companies`：公司基础资料。
- `company_node_mappings`：公司与产业环节的映射及证据。

所有业务表都必须包含 `workspace_id` 或可追溯到工作空间，避免多人使用后发生越权读取。

## 年报与研报处理流程

1. 客户端申请上传地址，将 PDF 直接上传对象存储。
2. 后端创建文档任务并立即返回任务 ID，客户端显示处理进度。
3. 异步任务提取 PDF 文本、页码、目录和表格；扫描件进入 OCR。
4. 文本按章节和页码切分，原文片段永久保留定位信息。
5. DeepSeek 只对相关片段进行结构化提取：公司业务、产品、产能、客户、收入、技术路线和风险。
6. 系统把提取结果转换成产业图谱补丁，并展示“原文—推断—拟修改内容”。
7. 用户逐项接受后才更新正式节点或公司映射。
8. 导出报告时，每个关键判断都能回到文件、页码和原文片段。

不要让模型直接阅读整份年报后输出一篇不可追踪的总结。精准研究依赖页码级原文、结构化字段和人工确认。

## 登录和权限

### 登录

- 小程序使用微信登录，后端用登录凭证换取用户身份并建立自己的会话。
- Web 端可增加手机号、邮箱或微信扫码登录。
- 客户端只保存短期会话，不保存 DeepSeek Key。

### 角色

- `owner`：管理空间、成员、账单和删除操作。
- `editor`：编辑图谱、资料、任务并接受 AI 修改。
- `viewer`：只读查看和导出允许的内容。

项目分享需要单独的只读链接和失效时间，不能把内部项目地址直接公开。

## DeepSeek 接入

- 正式请求统一走后端 `/api/ai/*`，前端不再直接访问模型服务。
- 默认模型使用 `deepseek-v4-flash`，适合拆解、摘要和问答。
- 高难度产业推理、年报综合分析可切换 `deepseek-v4-pro`。
- 所有结构修改继续使用补丁协议，模型不能直接覆盖项目。
- 记录输入、输出、模型、Token、费用、提示版本和错误，便于审计与优化。
- 对 JSON 返回做服务端 Schema 校验；失败时重试或进入人工处理，不把损坏结果写入数据库。

## 精准导出

- Markdown：快速复制和版本管理。
- Word/PDF：正式研究报告，保留目录、图表、脚注和来源列表。
- Excel：节点、关系、公司、指标和证据明细。
- PNG/SVG：产业图谱全景。
- 分享网页：只读、可控制权限和有效期。

服务端导出必须固定报告模板，并把结论引用转换为脚注；图谱截图由服务端或前端生成后作为报告附件。

## 推荐开发顺序

### 阶段一：后端基础

- 建立 FastAPI、PostgreSQL、用户会话和工作空间。
- 实现项目、节点、关系、资料、任务和快照 API。
- 支持导入当前原型导出的 JSON。
- 将 DeepSeek 请求迁移到后端代理。

### 阶段二：正式 Web 端

- React/TypeScript + React Flow 工程、统一类型层、API 客户端和生产构建已经完成。
- 已迁移图谱拖拽、研究卡片、自动保存、旧数据迁移、研究看板、公司研究、文件任务读取和工作空间同步。
- PDF 上传、解析轮询、DeepSeek 年报发现审核、页码引用写回、正式版快照恢复和 Word/Excel/PDF 精准导出已经完成迁移。
- 正式版后端连接、开发登录、证据引用编辑、本地 OCR 操作、年报表格预览/CSV/节点写入已经完成迁移。
- 产业图谱 AI 结构化补丁、修改前后差异、选择性接纳，以及研究任务新增/编辑/删除/状态推进/缺口生成已经完成迁移。
- 正式邮箱注册/登录、工作空间成员管理、owner/editor/viewer 角色界面和项目乐观锁冲突处理已经完成。
- 云端项目中心、跨研究空间切换、已有项目重新打开、账号/空间/项目级本地数据隔离和 viewer 只读界面已经完成。
- 正式 Web 核心研究与基础多人协作闭环已经完成；下一批进入部署基础设施、账号恢复流程与小程序适配。

### 阶段三：文件研究

- 文件上传、对象存储和解析任务状态。
- PDF 文本、页码、表格与 OCR。
- DeepSeek 结构化提取、原文引用和补丁审阅。
- Word/PDF/Excel 精准导出。

### 阶段四：微信小程序

- 原生微信小程序工程、本地体验、邮箱注册/登录、微信登录、邮箱账号绑定微信、研究空间切换和云端项目列表已经完成。
- 移动端产业链层级视图、搜索、完整节点研究卡片编辑、节点增删、手工证据、证据核验、任务创建与推进和 viewer 只读模式已经完成。
- 项目版本同步、自动云端保存与本地/云端冲突处理已经完成。
- PDF 选择上传、解析/OCR 状态、DeepSeek 年报提取和逐条移动审核写回已经完成。
- 产业图谱 AI 新建、AI 补丁移动审核、修改前后对照和选择性接纳已经完成。
- 公司档案与产业映射审核、成员权限管理、Word/Excel/PDF 精准导出已经完成。
- 代码层小程序功能闭环已经完成；剩余工作是申请正式 AppID、部署 HTTPS 后端、配置合法域名并进行真机与微信审核验证。
- 大图谱在小程序中使用简化层级视图；复杂布局仍由 Web 端承担。

### 阶段五：公司映射（基础闭环已完成）

- 从年报提取公司业务和经营数据。
- 公司映射到一个或多个产业节点，并保留原文证据。
- 支持从节点查看公司、从公司查看产业覆盖范围。
- 当前已支持本地候选生成、匹配理由、人工确认/排除、手动公司与映射、多期年报、经营指标时间序列、2–4 家公司横向对比、双向查看和报告导出。
- 已支持公司主档 JSON 的跨项目导入、名称去重合并和产业节点重映射；工作空间级服务端公司主档、项目来源追踪、自动上送与主动拉取也已完成。
- 已支持待确认映射的集中搜索、全选、批量确认和批量排除；下一步接入 DeepSeek 语义候选与正式异步任务队列。

## 下一开发里程碑

后端最小骨架、正式账号、工作空间成员权限、带冲突检测的项目同步、JSON 导入、PDF 页码级解析、DeepSeek 代理和年报结构化提取审核已经形成闭环。

下一里程碑是把文件研究做得适合真实年报：

1. 完善已落地的本地 OCR 和表格结构提取：增加扫描表格、区域重识别和人工纠错；
2. 解析任务队列、重试、取消和历史任务列表；
3. 年报发现与产业节点的批量映射建议；
4. 完善已落地的 Word/PDF/Excel 精准导出：增加公司模板、图谱插图和正式脚注编号；
5. PostgreSQL、对象存储和正式微信登录，为小程序版做准备。
